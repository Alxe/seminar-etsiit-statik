# views/project-info.yml
path:
  template: "/projects/{{ project.pk }}"
  for-each:
    project: session.query(Project).filter(Project.active).all()

template: "project-info"
context:
  static:
    section-title: "Project Info"